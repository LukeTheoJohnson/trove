# trove data dictionary

trove is a hoarding engine: every fetch appends a **timestamped observation** to a local SQLite
store, so the cache compounds into a proprietary time-series you can't buy or backfill. This file
documents the store and the `export` output so the hoard is consumable by other tools.

## Store (SQLite, `data/<source>.db`)

One generic schema serves every source. Source-specific fields ride in JSON columns (`extra` on
items, `flags` on observations), so a new source needs no migration.

### `items` — one row per tracked entity
| column | type | meaning |
|--------|------|---------|
| `item_id` | TEXT (PK) | the **join key** (appid, release id, card id, station id...) |
| `name` | TEXT | display name |
| `subtitle` | TEXT | source-defined secondary label (artist, set, card type...) |
| `category` | TEXT | source-defined class (genre, format, rarity...) |
| `extra` | TEXT (JSON) | source-specific metadata (see per-source table below) |
| `first_seen` / `last_seen` | TEXT (UTC `YYYY-MM-DD HH:MM:SS`) | first/most-recent time the item was written |

### `obs` — the time-series (one row per observation; **never updated, only appended**)
| column | type | meaning |
|--------|------|---------|
| `id` | INTEGER (PK) | autoincrement |
| `item_id` | TEXT | FK to `items` |
| `ts` | TEXT (UTC) | observation timestamp |
| `price_cents` | INTEGER | headline price in cents, in the source's denomination (see below); `0` = free, `NULL` = no price |
| `was_cents` | INTEGER | reference/list price where the source provides one |
| `qty` | INTEGER | quantity/availability signal (e.g. discogs `num_for_sale`) |
| `flags` | TEXT (JSON) | source-specific signals (see per-source table) |
| `tag` | TEXT | what wrote the row: `search` / `item` / `poll` / `hist` (backdated rows merged from a deep-history payload — `ts` is the source's own historic date, inserted idempotently so re-polling never duplicates them) |

### `watch` — the watchlist
| column | meaning |
|--------|---------|
| `item_id` | FK to `items` |
| `added_at` | UTC timestamp |

## `export` command

```bash
python trove.py <source> export                 # full obs time-series -> data/<source>_obs.csv
python trove.py <source> export --what latest    # newest row per item (snapshot)
python trove.py <source> export --what items     # the item catalog
python trove.py <source> export --out merged.csv # custom path
```

UTF-8 CSV, fully offline. Every row is prefixed with a `source` column so exports from multiple
sources concatenate cleanly into one hoard. `obs`/`latest` columns:
`source,item_id,name,subtitle,category,ts,price_cents,was_cents,qty,tag,flags,first_seen,last_seen`.
`items` columns: `source,item_id,name,subtitle,category,extra,first_seen,last_seen`. The `flags` and
`extra` cells are raw JSON strings — parse them per the table below.

## Per-source semantics (denomination, `flags`, `extra`)

Grouped by genre (same sections as the `--help` listing and the backlog).

### games / media / collectibles
| source | `price_cents` denomination | `flags` keys | `extra` keys |
|--------|----------------------------|--------------|--------------|
| steam | store currency via `--cc` (final price) | `discount_pct`, `is_free` | `metascore` (search); `release`, `platforms` (item) |
| discogs | `--cc` curr (lowest marketplace price); `qty` = num for sale | _(none)_ | `year`, `have`, `want` |
| itunes | `--cc` country store | _(none)_ | `kind` |
| scryfall | `--cc` = `usd`\|`eur`\|`tix` (nonfoil) | `foil_cents` | `set`, `rarity`, `released` |
| pokemontcg | `--cc` = `usd` (TCGplayer market) \| `eur` (Cardmarket trend) | `denom`, `variant`, `low_cents`, `mid_cents`, `high_cents`, `avg30_cents`, `avg7_cents`, `updated` | `set`, `series`, `released` |
| ygoprodeck | `--cc` = tracked venue (`tcgplayer` default) | `denom`, `tcgplayer_c`, `cardmarket_c`, `ebay_c`, `amazon_c`, `coolstuffinc_c` | `attribute`, `atk`, `def`, `level`, `archetype`, `sets` |
| epic | country/currency via `--cc` (`nz` -> NZD); effective price (`0` while free), `was_cents` = RRP | `free`, `upcoming`, `start`, `end`, `currency` | `desc`, `url`, `image`, `orig_fmt`, `currency`, `namespace` |
| steammarket | USD cents (search `sell_price`, integer); `--cc` = Steam currency code (default `1`=USD) localises the `item`/`poll` priceoverview `lowest_price` only (search is USD-only); `qty` = listing depth (`sell_listings`) on **search** rows / 24h `volume` on **item+poll** rows (see `flags.src`) | `src` (`search`/`overview`), `listings`, `median_cents`, `volume`, `currency`, `median_text` | `app`, `hash_name`, `icon`, `url`, `tradable` |

### fuel & electricity
| source | `price_cents` denomination | `flags` keys | `extra` keys |
|--------|----------------------------|--------------|--------------|
| spainfuel | EUR cents/L for the headline grade G95E5 | `grade`, `board` (all grades cents/L), `milli`, `area_avg` | `municipio`, `cp`, `lat`, `lon`, `horario`, `idees` |
| petrolspy | NZ/AU cents/L for the headline grade | `grade`, `board`, `updated`, `relevant`, `area_avg`, `unit` | `suburb`, `postcode`, `lat`, `lon`, `open24`, `country`, `brand` |
| em6 | NZ wholesale electricity spot, $/MWh * 100 (so `drops` = price *falling*) | `unit` ($/MWh), `trading_period`, `timestamp`, `nat_avg` | `grid_zone_id` |
| octopus | UK Agile unit rate, p/kWh inc VAT * 100 (centi-pence; can be **negative** = plunge pricing), so `drops` = electricity *getting cheaper*; deal = at/below today's avg or a negative rate. money() cosmetically renders the rate as dollars in the watchlist + poll DROP line only (em6/geonet precedent) | `unit` (p/kWh), `basis` (`headline` from search / `half_hour` from item+poll), `day_avg`, `next_rate`, `valid_from`, `valid_to`, `plunge` | `gsp_group` |
| aemo | AU NEM 5-minute dispatch spot price, $/MWh * 100 (can be **negative** at high renewable output), so `drops` = price *falling*; `qty` = total demand (MW); deal = at/below the five-region average this interval. money() renders $/MWh in the watchlist + poll DROP line (em6 precedent) | `unit` ($/MWh), `price_status`, `demand_mw`, `settlement`, `scheduled_gen`, `semischeduled_gen`, `net_interchange`, `interconnectors` (list of `{name, value}` MW), `nem_avg` | `region_id` |
| fuelwatch | WA pump price in whole cents/L for ULP (money() shows $/L; exact tenth-cent in `flags.price_c`), so `drops` = the station getting cheaper; deal = at/below the station's **suburb** average today | `product` (ULP), `unit` (cents/L), `price_c` (exact), `brand`, `suburb`, `suburb_avg`, `date`, `restrictions` | `brand`, `suburb`, `address`, `phone`, `features`, `lat`, `lon` |
| awattar | Germany/Austria EPEX day-ahead spot, Eur/MWh * 100 (can be **negative** = renewable-oversupply plunge), so `drops` = the price *falling*; `price_cents` = the hour covering **now**. `item` seeds ~90 days of hourly prices as backdated `hist` rows (each backdated to its delivery hour); `poll` reads the current + next-day window and appends only unseen hours; deal "cheap" = at/below the returned window's average. `--cc` = `de` (default) or `at`. money() renders Eur/MWh as $ in the 2 hardcoded spots | `unit` (Eur/MWh), `window_avg`, `hours`, `current_from`; `hist` rows carry only `unit` | `market` |
| carbonintensity | GB regional grid **carbon intensity forecast**, gCO2/kWh * 100 (61 gCO2 -> 6100), so `drops` = the grid getting *greener*; `qty` = renewable share % (wind+solar+hydro+biomass); deal "clean" = index "very low"/"low". Forecast-as-issued (drift; realized history is archived but the as-issued forecast is not). money() renders centi-intensity as $ in the 2 hardcoded spots | `gco2_kwh`, `index` (very low..very high), `renewable_pct`, `mix` (fuel -> %) | `dnoregion` |
| nyiso | `price_cents` = **NYISO real-time zonal LBMP ($/MWHr) × 100** (a real $ value, so money() reads true), `drops` = the zonal price *falling*; `qty` = None; deal "deal" = at/below the all-zone average this 5-min interval. Join key = zone Name; one GET of the day's `realtime_zone.csv` serves all zones, latest interval per zone. Low-med hoard (NYISO archives settlement — the octopus/awattar class; completes US electricity) | `unit` ($/MWHr), `lbmp`, `losses`, `congestion`, `timestamp`, `ny_avg` | `zone`, `ptid` |
| francefuel | `price_cents` = the tracked grade's price in **euro-cents** (€1.729 → 173), `drops` = the pump getting cheaper; `qty` = count of grades sold; deal "cheap" = at/below the national sample average for that grade. `--cc` picks the grade (gazole/sp95/sp98/e10/e85/gplc). ODS caps `limit` at 100 (melbped lesson). money() shows a '$' glyph but the value is €. **High** hoard (per-station forecourt price, overwritten in place, never archived — spainfuel/petrolspy twin) | `grade`, `unit` (euro-cents), `grade_avg`, `prices` (grade→cents), `maj`, `available`, `rupture` | `ville`, `cp`, `adresse`, `departement`, `region`, `lat`, `lon` |

### currency & macro
| source | `price_cents` denomination | `flags` keys | `extra` keys |
|--------|----------------------------|--------------|--------------|
| frankfurter | ECB reference rate * 10,000 (ten-thousandths, pip-style: 0.56709 stores as 5671), so `drops` = the base currency *weakening*; `qty` = the current rate's percentile (0-100) within the trailing year; deal "high" = percentile >= 90. `item` seeds the **full daily series since 1999-01-04** as backdated `hist` rows (bare `price_cents` + `ts`, no flags); `poll` re-reads a trailing ~400-day window and appends only unseen dates. money() cosmetically renders the scaled rate as dollars in the watchlist + poll DROP line only (octopus/geonet precedent) | `rate`, `date` (ECB fixing date), `pctile_1y`, `lo_1y`, `hi_1y` (floats), `n_days`, `since`, `src` (`series` from item+poll / `latest` from search) | `base`, `quote`, `base_name`, `quote_name` |
| paralelobo | `price_cents` = the **parallel-market median USD/BOB rate × 100** (centi-BOB; 10.53 → 1053), so `drops` = the parallel rate *easing* (boliviano strengthening); `qty` = the count of P2P sources aggregated; deal "premium" = the parallel rate ≥ 40% over the official 6.96 peg (`premium` ≥ 1.40). One aggregate item (join key `usd`). **Un-rebuildable** — no one archives the parallel series (unlike the frankfurter/ECB official record). money() renders centi-BOB as $ (the value is BOB) | `buy`, `sell`, `median`, `spread_pct`, `premium` (×peg), `official_peg`, `sources` (csv), `timestamp` | `official_peg` |

### deals, fares & listings
| source | `price_cents` denomination | `flags` keys | `extra` keys |
|--------|----------------------------|--------------|--------------|
| grabone | NZD deal price; `was_cents` = RRP (listing rows) | `currency`, `region`, `discount_pct`, `available`, `src` (`listing`/`detail`), `valid_through` (detail) | `url`, `image`, `merchant`, `region`; detail adds `valid_from` |
| grabaseat | NZD cheapest fare for the route (* 100) | `currency`, `lowest_date`, `avg_cents`, `min_cents`, `max_cents`, `n_fares`, `board` (per-day fares) | `origin`, `destination`, `book_url` |
| bookme | NZD deal price; `was_cents` = price+save; `qty` = spaces remaining | `currency`, `discount_pct`, `save_cents`, `region`, `date_from`, `date_to` | `url`, `region`, `rating`, `reviews` |
| turners | NZD asking price; `was_cents` = RRP where discounted | `currency`, `channel`, `discounted`, `discount_pct`, `odometer_km`, `branch`, `availability` | `make`, `model`, `year`, `odometer_km`, `branch`, `fuel`, `body`, `channel`, `url` |
| eventcinemas | **not money** — `qty` = seats remaining (no price in feed); deal = a session near sellout | `cinema`, `cinema_id`, `screen_type`, `screen`, `start`, `attributes`, `reserved_seating`, `sold_out` | `movie`, `movie_id`, `movie_url`, `rating`, `cinema`, `screen_type`, `screen`, `start`, `date`, `booking_url` |
| reverb | `--cc` display currency (default NZD, via `X-Display-Currency`) — effective checkout price (`buyer_price`); `was_cents` = list `price` when marked down; `qty` = inventory; deal = a live seller markdown | `state`, `condition`, `sale`, `ribbon`, `currency`, `offers`, `auction` | `model`, `year`, `finish`, `shop`, `url`, `image`, `currency` |

### attention & rank
| source | `price_cents` denomination | `flags` keys | `extra` keys |
|--------|----------------------------|--------------|--------------|
| hackernews | **not money** — `price_cents` = front-page **rank * 100** (centi-rank, rank 1 = top), so `drops` = a story *climbing*; `NULL` once it falls off the top-500 (obs then ride on `qty`); `qty` = comment count; deal = rank <= 10. money() renders centi-rank as $ in the watchlist + poll DROP line only (geonet/metno precedent) | `rank`, `score`, `comments`, `by`, `age_h`, `type` | `title`, `by`, `url`, `domain`, `posted`, `hn_url` |
| appcharts | **not money** — `price_cents` = chart **rank * 100** (centi-rank), so `drops` = an app *climbing*; an app off the chart ends its series (fetch returns nothing); deal = rank <= 10; `--cc` = storefront country (default nz, embedded in the join key). money() renders centi-rank as $ in the 2 hardcoded spots | `rank`, `chart`, `country`, `artist`, `updated` | `appid`, `artist`, `chart`, `country`, `released`, `url`, `icon` |
| melbped | **not money** — `price_cents` = latest per-minute **pedestrian count * 100** (centi-count), so `drops` = a spot *quietening*; `qty` = the raw count/min; deal "busy" = at/above the current network median footfall. money() renders the centi-count as $ in the 2 hardcoded spots (Melbourne Opendatasoft `location_id` join key) | `count`, `dir1`, `dir2`, `at` (sensing datetime), `snapshot_median` | `location_id`, `sensor`, `lat`, `lon`, `type`, `status`, `dir1_label`, `dir2_label` |

### weather, environment & geohazard
| source | `price_cents` denomination | `flags` keys | `extra` keys |
|--------|----------------------------|--------------|--------------|
| geonet | **not money** — `price_cents` = magnitude * 100 (centi-magnitude), so `drops` = a quake *downgraded* on review; `qty` = MMI | `magnitude`, `mmi`, `depth_km`, `quality`, `locality`, `time` | `lat`, `lon`, `locality`, `time`, `url` |
| metno | **not money** — `price_cents` = upcoming-day forecast **high** in centi-degrees C (22.0C -> 2200), so `drops` = a forecast that *cooled*; `qty` = that day's forecast rain in tenths of a mm | `high_c`, `low_c`, `precip_mm`, `symbol`, `wind_ms`, `target_date`, `issued` | `lat`, `lon`, `name`, `issued`, `target_date`, `current_c`, `current_symbol`, `outlook`, `url` |
| volcano | **not money** — `price_cents` = Volcanic Alert Level * 100 (centi-level, 0-5), so `drops` = a *de-escalation*; `qty` = raw level 0-5 | `level`, `acc` (colour), `activity`, `hazards` | `lat`, `lon`, `activity`, `hazards`, `url` |
| nzski | **not money** — `price_cents` = headline base depth cm * 100, so `drops` = the base *melting*; `qty` = lifts open count | `base_cm`, `base_min`, `base_max`, `season_total`, `last7days`, `lifts_open`, `lifts_total`, `trails_open`, `trails_total`, `status`, `temp_high`, `temp_low`, `updated` | `slug`, `updated`, `road_status`, `chain_status`, `weather`, `url` |
| gwrivers | **not money** — `price_cents` = latest **Flow** m3/s * 100 (centi-cumecs) **or** Stage mm * 100 (per-site consistent; read `flags.measurement`/`unit`), so `drops` = a river *receding*; deal `rising` is precomputed in flags | `measurement`, `unit`, `value`, `value_24h_ago`, `max_24h`, `min_24h`, `change_24h`, `pct_change_24h`, `rising`, `latest_time` | `measurement`, `unit`, `url` |
| avalanche | **not money** — `price_cents` = **headline avalanche danger × 100** (centi-danger: max of the three elevation-band ratings on the 1-5 scale, 1 Low → 5 Extreme; negative band ratings are "not rated" sentinels, excluded), so `drops` = the danger *easing*; `qty` = number of avalanche problems posted; deal "danger" = headline ≥ 3 (Considerable). The danger is issued daily per region and revised as observations accrue — the un-rebuildable forecast-drift series (metno/spaceweather class); a region with no current forecast (off-season) ends/pauses its series. money() renders centi-danger as $ in the watchlist + poll DROP line only (geonet/volcano precedent) | `danger`, `danger_name`, `bands` (list of `{label, rating, from, to}` for Alpine/Sub-alpine/Below treeline), `trend`, `problems`, `confidence`, `forecaster`, `valid`, `issued`, `edited` | `region_id`, `urlSegment`, `lat`, `lon`, `url`, `metservice` |
| mdcrivers | **not money** — `price_cents` = latest **Flow** m3/s * 100 (centi-cumecs) **or** Stage mm * 100 (per-site consistent; read `flags.measurement`/`unit`), so `drops` = a river *receding*; deal `rising` (latest >= 1.5x its 24h-ago value) is precomputed in flags. Marlborough District Council Hilltop; same shape as `gwrivers` | `measurement`, `unit`, `value`, `value_24h_ago`, `max_24h`, `min_24h`, `change_24h`, `pct_change_24h`, `rising`, `latest_time` | `measurement`, `unit`, `url` |
| horizonsrivers | **not money** — same model as `mdcrivers`/`gwrivers` but Horizons (Manawatu-Whanganui) Hilltop: `price_cents` = latest Flow m3/s * 100 or Stage mm * 100, `drops` = receding, deal `rising` = flood onset | `measurement`, `unit`, `value`, `value_24h_ago`, `max_24h`, `min_24h`, `change_24h`, `pct_change_24h`, `rising`, `latest_time` | `measurement`, `unit`, `url` |
| northlandrivers | **not money** — same model as `gwrivers`/`mdcrivers`/`horizonsrivers` but Northland Regional Council Hilltop (subtropical, flood-prone far north; ~1100 gauge sites): `price_cents` = latest Flow m3/s * 100 or Stage mm * 100, `drops` = receding, deal `rising` = flood onset | `measurement`, `unit`, `value`, `value_24h_ago`, `max_24h`, `min_24h`, `change_24h`, `pct_change_24h`, `rising`, `latest_time` | `measurement`, `unit`, `url` |
| westcoastrivers | **not money** — same model as `gwrivers`/`mdcrivers`/`horizonsrivers` but West Coast Regional Council Hilltop (wettest NZ region, flashy alpine rivers; ~120 gauge sites): `price_cents` = latest Flow m3/s * 100 or Stage mm * 100, `drops` = receding, deal `rising` = flood onset | `measurement`, `unit`, `value`, `value_24h_ago`, `max_24h`, `min_24h`, `change_24h`, `pct_change_24h`, `rising`, `latest_time` | `measurement`, `unit`, `url` |
| nswrfs | **not money** — `price_cents` = **alert-level ordinal × 100** (Emergency Warning=300, Watch and Act=200, Advice=100, Planned Burn / N.A.=0), so `drops` = a fire *de-escalating*; an incident gone from the feed = resolved (fetch returns nothing); `qty` = size in hectares; deal "bushfire" = out-of-control fire at Watch and Act+. money() renders the centi-ordinal as $ in the 2 hardcoded spots | `alert_level`, `status`, `fire`, `type`, `size`, `council`, `agency`, `updated`, `category` | `location`, `council`, `agency`, `type`, `lat`, `lon`, `link` |
| vicemergency | **not money** — `price_cents` = **alert-level ordinal × 100** (Emergency Warning=300, Watch and Act=200, Advice=100, else 0), so `drops` = a warning *de-escalating*; a warning gone = resolved; deal "warning" = Watch and Act+. All-hazards (fire/flood/storm). money() renders the centi-ordinal as $ in the 2 hardcoded spots | `alert_level`, `hazard`, `status`, `action`, `feed_type`, `statewide`, `source_org`, `updated` | `hazard`, `feed_type`, `source_org`, `location`, `lat`, `lon` |
| sacfs | **not money** — `price_cents` = incident **response Level × 100**, so `drops` = an incident *de-escalating*; an incident gone = closed; `qty` = appliances committed; deal "active" = status GOING. money() renders the centi-level as $ in the 2 hardcoded spots | `type`, `status`, `level`, `region`, `resources`, `aircraft`, `date`, `time`, `message` | `type`, `location`, `region`, `lat`, `lon`, `link` |
| beachwatch | **not money** — `price_cents` = latest water-quality **star rating × 100** (1 Poor .. 4 Good), so `drops` = water quality *worsening*; deal "pollution" = the daily pollution forecast is Possible/Likely (swim advisory). money() renders the centi-rating as $ in the 2 hardcoded spots | `pollution_forecast`, `forecast_at`, `latest_result`, `rating`, `observed` | `site_id`, `lat`, `lon` |
| safeswim | **not money** — `price_cents` = water-quality **safety ordinal × 100** (GREEN=300, RED=200, RED+=100, BLACK=0; None = ungraded), so `drops` = water quality *worsening* (a beach flipping GREEN→RED after rain); deal "alert" = quality RED/RED+/BLACK (don't-swim). money() renders the centi-ordinal as $ in the 2 hardcoded spots | `quality`, `patrolled` | `slug`, `patrolled`, `alt_name`, `lat`, `lon`, `url` |
| eafloods | **not money** — `price_cents` = **impact ordinal × 100** (`(5 − severityLevel) × 100`: Severe Flood Warning=400, Flood Warning=300, Flood Alert=200, no-longer-in-force=100), so `drops` = a warning *de-escalating*; an area gone from the feed = stood down (fetch returns nothing, the series ends); deal "flood" = severityLevel ≤ 2 (Flood Warning / Severe). Event-driven — the feed is often empty in dry spells (avalanche off-season pattern). money() renders the centi-ordinal as $ in the 2 hardcoded spots | `severity` (1-4), `severity_label`, `message`, `tidal`, `raised`, `changed` | `area_id`, `county`, `river`, `tidal`, `url` |
| usgs | **not money** — `price_cents` = **latest flow (ft3/s) or gauge height (ft) × 100**, so `drops` = a river *receding*; `qty` = null; the flood event is a *rise*, computed at fetch and stored `rising` = latest ≥ 1.5× value 24h ago (gwrivers pattern); deal "rising" reads that flag. USGS `-999999` no-data sentinels are dropped. money() renders the centi-scalar as $ in the 2 hardcoded spots | `measurement`, `unit`, `value`, `value_24h_ago`, `max_24h`, `min_24h`, `change_24h`, `pct_change_24h`, `rising`, `latest_time`, `other`, `other_value` | `measurement`, `unit`, `site`, `url` |
| wildfire | **not money** — `price_cents` = **incident size (acres) × 100**, so `drops` = a fire revised *down*; `qty` = percent contained; deal "major" = type WF & ≥ 1000 acres & < 50% contained. Join key = `IrwinID`; an incident gone from the current layer = out (fetch returns nothing, the series ends). money() renders centi-acres as $ in the 2 hardcoded spots | `acres`, `contained_pct`, `cause`, `type` (WF/RX), `state`, `county`, `discovered` | `state`, `county`, `type`, `kind`, `lat`, `lon`, `url` |
| airquality | **not money** — `price_cents` = **PM2.5 (or PM10 fallback) µg/m3 × 100** (centi-microgram), so `drops` = the air *cleaning*; `qty` = PM10; deal "unhealthy" = PM2.5 ≥ 25 µg/m3. Join key = sensor id; only sensors reporting P1/P2 are kept. money() renders the centi-scalar as $ in the 2 hardcoded spots | `pm25`, `pm10`, `sensor_type`, `country`, `time`, `temperature`, `humidity` | `sensor_type`, `country`, `lat`, `lon`, `url` |
| usgsquakes | **not money** — `price_cents` = **magnitude × 100** (centi-magnitude, the geonet scalar), so `drops` = an event *downgraded* on review; `qty` = USGS "significance" score; deal "strong" = M ≥ 4.5 or a tsunami flag. Join key = USGS event id; a recent-feed query + a by-id `eventid` fetch. Low hoard (USGS keeps the authoritative catalogue — the octopus/frankfurter class; global geonet twin). money() renders centi-magnitude as $ in the 2 hardcoded spots | `mag`, `place`, `time`, `updated`, `mmi`, `cdi`, `felt`, `alert`, `tsunami`, `sig`, `depth_km`, `status` | `lat`, `lon`, `depth_km`, `url`, `magType` |
| hkweather | **not money** — `price_cents` = the day's **forecast max temp × 100** (centi-degree C), so `drops` = a forecast *cooling*; `qty` = forecast min temp; deal "hot" = max ≥ 33 C **or** any HKO warning in force. Join key = `YYYYMMDD` forecast date (metno/spaceweather pattern); one pass = 2 memoized GETs (`fnd` forecast + `warnsum` warnings). Forecast-as-issued = un-rebuildable (HKO archives realized weather only). money() renders centi-degrees as $ in the 2 hardcoded spots | `max_c`, `min_c`, `weather`, `wind`, `psr` (rain prob text), `max_rh`, `min_rh`, `week`, `warnings` (csv, "" when quiet) | `date`, `week` |
| ipma    | **not money** — `price_cents` = the **next-day forecast max temp × 100** (centi-degree C), so `drops` = a forecast *cooling*; `qty` = precipitation probability %; deal "rain" = precip prob ≥ 70%. Join key = IPMA `globalIdLocal` (names from the memoized `distrits-islands` list); tracks the `day1` (tomorrow) forecast, re-read each poll (metno drift). money() renders centi-degrees as $ in the 2 hardcoded spots | `max_c`, `min_c`, `rain_prob`, `weather` (idWeatherType→text), `wind_dir`, `wind_class`, `forecast_date` | `lat`, `lon`, `forecast_date` |

### space
| source | `price_cents` denomination | `flags` keys | `extra` keys |
|--------|----------------------------|--------------|--------------|
| spaceweather | **not money** — `price_cents` = a UTC day's **peak forecast Kp** * 100 (centi-Kp, 0-900), so `drops` = a day's peak-Kp forecast revised *down* (storm calming); `qty` = count of 3-hour periods that day at Kp>=5. money() renders centi-Kp as $ in the watchlist + poll DROP line only (geonet/metno/volcano precedent) | `peak_kp`, `peak_time_utc`, `status` (observed/predicted/mixed), `storm_periods`, `scale` (NOAA G-scale), `aurora` | `peak_time_utc`, `scale`, `n_periods` |
| sentry | **not money** — `price_cents` = cumulative **Palermo scale * 100** (centi-Palermo, negative), so `drops` = an object's impact risk revised *down*; an object retired from the risk list ends its series (fetch returns nothing); `qty` = count of potential impacts (`n_imp`); deal = Torino >= 1 or Palermo >= -2. money() renders centi-Palermo as (negative) $ in the 2 hardcoded spots | `ps_cum`, `ps_max`, `ts_max`, `ip`, `n_imp`, `diameter_km`, `last_obs`, `src` (`list`/`detail`) | `diameter_km`, `h_mag`, `last_obs`, `range`, `v_inf_kms`; detail adds `first_obs`, `n_obs`, `mass_kg`, `energy_mt`, `v_imp_kms`, `computed`, `method`, `n_vi`, `nearest_imp` |
| spacelaunch | **not money** — `price_cents` = a **readiness ordinal × 100** (Go=4000, TBC=3000, TBD=2000, Hold=1000; terminal/flown = NULL), so `drops` = a launch slipping *off* readiness (Go->Hold); `qty` = whole hours until `net`; deal "go" = Go/TBC within 24h. Every obs stamps `net`+`net_precision` so the as-scheduled NET slip is captured for export; a launch leaving `upcoming` (it flew) ends its series. money() renders the centi-ordinal as $ in the 2 hardcoded spots | `status`, `abbrev`, `net`, `net_precision`, `provider`, `orbit` | `provider`, `pad`, `location`, `url` |

### aviation
| source | `price_cents` denomination | `flags` keys | `extra` keys |
|--------|----------------------------|--------------|--------------|
| chcflights | **not money** — `price_cents` = **delay in minutes** (estimate − scheduled; negative = early, `0` = currently expected on time), so `drops` = a flight that *recovered* (delay shrank); deal = delayed ≥ 15 min or cancelled. money() cosmetically renders the delay as dollars in the watchlist + poll DROP line only | `status`, `gate`, `estimate`, `scheduled`, `delay_min`, `cancelled`, `delayed`, `route`, `direction`, `type`, `last_updated` | `flight_no`, `codeshares`, `airline`, `airline_code`, `route`, `direction`, `type`, `scheduled`, `image_url` |
| zqnflights | **not money** — `price_cents` = **delay in minutes** (estimate − scheduled, an honest datetime diff — ZQN serves full ISO date+time pairs; `0` = currently expected on time), so `drops` = a flight that *recovered*; deal = delayed ≥ 15 min or cancelled. Same money() cosmetic as chcflights | `status`, `scheduled`, `estimate`, `delay_min`, `cancelled`, `delayed`, `route`, `direction`, `domestic` | `flight_no`, `codeshares`, `from`, `to`, `route`, `direction`, `domestic`, `scheduled` |
| opensky | **not money** — `price_cents` = **barometric altitude in metres** (10 000 m -> $100.00), so `drops` = an aircraft *losing altitude* (descending on approach); `qty` = ground speed (m/s); deal "descending" = airborne, <3000 m and vertical rate < 0. `--cc` picks the bounding box (nz/au/uk/nyc/sf/la), a plane leaving the box ends its series. Anonymous OpenSky = live snapshot only (history needs a contributor account = un-rebuildable for us). money() renders altitude-metres as $ in the 2 hardcoded spots | `callsign`, `country`, `lat`, `lon`, `alt_m`, `speed_ms`, `heading`, `vrate`, `on_ground`, `squawk` | `country`, `callsign` |
| adsblol | **not money** — `price_cents` = **barometric altitude in feet** (35 000 ft -> $350.00), so `drops` = an aircraft *descending*; `qty` = ground speed (kt); deal "descending" = airborne, <10 000 ft and baro_rate < 0. `--cc` picks the region centre+radius (lon/nz/au/syd/nyc/la/sf); `alt_baro` can be the string "ground" (→ 0, on-ground). Join key = ICAO `hex`; a plane out of range ends its series. Keyless-community opensky twin. money() renders altitude-feet as $ in the 2 hardcoded spots | `callsign`, `registration`, `type`, `lat`, `lon`, `alt_ft`, `speed_kt`, `heading`, `baro_rate`, `on_ground`, `squawk`, `dist_nm` | `registration`, `type`, `callsign` |

### roads & transport
| source | `price_cents` denomination | `flags` keys | `extra` keys |
|--------|----------------------------|--------------|--------------|
| nzroads | **not money** — `price_cents` = **impact severity × 100** (centi-severity: Road Closed=400, Vehicle Restrictions=300, Delays=200, Caution=100, info=0), so `drops` = a *de-escalation* (the road recovering); an event gone from the feed = resolved (fetch returns nothing, the series ends); `qty` = regions touched; deal = unplanned + active + ≥ Delays. money() renders centi-severity as $ in the watchlist + poll DROP line only (geonet/volcano precedent) | `impact`, `severity`, `type`, `status`, `planned`, `critical`, `description`, `comments`, `expected`, `last_edited` | `event_type`, `description`, `location`, `island`, `planned`, `source`, `start`, `regions`, `lat`, `lon`, `url` |
| tfl | **not money** — `price_cents` = a **health ordinal × 100** (Good Service/No Issues=1000 down to Suspended/Closed=100; a line takes the *worst* of its statuses), so `drops` = a line's service *worsening*; `qty` = count of active (non-good) statuses; deal "disruption" = any non-Good-Service (health < 10). money() renders the centi-ordinal as $ in the 2 hardcoded spots | `status`, `health` (1-10), `mode`, `reasons` | `mode`, `url` |
| mbta | **not money** — `price_cents` = alert **severity × 100** (MBTA's 0-10 scale, higher = worse), so `drops` = an alert's severity *easing*; an alert gone from the feed = resolved; `qty` = number of routes informed; deal "disruption" = a serious effect (SUSPENSION/SHUTTLE/CANCELLATION/STATION_CLOSURE/...) or severity ≥ 7. money() renders centi-severity as $ in the 2 hardcoded spots | `severity`, `effect`, `cause`, `lifecycle`, `routes`, `header` | `url`, `service_effect` |
| swisstransport | **not money** — `price_cents` = **delay in minutes** (signed; `0` = on time, following chcflights), so `drops` = a departure that *recovered* (delay fell); `qty` = whole minutes until scheduled departure; deal "delayed" = ≥ 3 min late. A departure leaving the board ends its series. Station is read back from the composite key on refresh. money() renders the delay-minutes as $ in the 2 hardcoded spots | `delay`, `platform`, `departure`, `line`, `to`, `station` | `station`, `line`, `to` |
| bcferries | **not money** — `price_cents` = the sailing's **fill percent** (how full, 0-100), so `drops` = a sailing *emptying* (cancellation/capacity reshuffle); `qty` = car-deck fill %; deal "fullrisk" = non-cancelled sailing ≥ 80% full. Join key = composite `DEP-DEST:time` (the scheduled slot's fill is one coherent series); one memoized GET of `/api/` = the whole board. Un-rebuildable per-sailing fill curve. money() renders fill-% as $ in the 2 hardcoded spots | `fill`, `car_fill`, `oversize_fill`, `cancelled`, `vessel`, `vessel_status`, `time`, `arrival`, `route` | `dep`, `dest`, `dep_name`, `dest_name`, `time`, `arrival` |

### shared mobility
| source | `price_cents` denomination | `flags` keys | `extra` keys |
|--------|----------------------------|--------------|--------------|
| bikeshare | **not money** — `price_cents` = **bikes available × 100** (centi-bike), so `drops` = a station that has *drained* below where first seen; `qty` = docks free (the returner's view); deal "stockout risk" = a renting station running dry (≤ 2 bikes). `--cc` picks the GBFS system (default `citibike`; 16 systems across US/CA/MX/EU — citibike/baywheels/capitalbikeshare/divvy/bluebikes/indego/metrobike/madison/boulder (US), bixi/torontobike (CA), ecobici (MX), oslobike/bergenbike/trondheimbike/warsawbike (EU); see `SYSTEMS`), embedded in the join key so mixed-system watchlists stay coherent; a station gone from the feed ends its series. money() renders centi-bike as $ in the watchlist + poll DROP line only (geonet/appcharts precedent) | `system`, `bikes`, `ebikes`, `docks`, `bikes_disabled`, `docks_disabled`, `capacity`, `renting`, `returning`, `installed`, `last_reported` | `system`, `station_id`, `short_name`, `region_id`, `capacity`, `lat`, `lon` |
| sgtaxi | **not money** — `price_cents` = the island-wide **available-taxi count × 100** (centi-taxi), so `drops` = the roaming fleet *thinning* (harder to hail); `qty` = the raw count; deal "scarce" = below 2,000 (tight supply). One aggregate item for the whole Singapore fleet. money() renders the centi-count as $ in the 2 hardcoded spots | `taxi_count`, `timestamp` | _(none)_ |

### parking
| source | `price_cents` denomination | `flags` keys | `extra` keys |
|--------|----------------------------|--------------|--------------|
| chcparking | **not money** — `price_cents` = **free spaces × 100** (centi-space), so `drops` = a Christchurch parking building *filling up*; `qty` = capacity (`free`+`occupied`, when sane); deal "fullrisk" = a **reliable** building ≤ 20 free spaces. The feed repeats some buildings (collapsed by `park_id`, first kept) and a failing sensor can report nonsense (negative `occupied`, absurd `free`) → logged faithfully but `reliable=False` so it never registers as a deal. money() renders centi-spaces as $ in the 2 hardcoded spots | `free`, `occupied`, `capacity`, `status`, `reliable` | `park_id` |
| sgcarpark | **not money** — `price_cents` = **free car (`C`) spaces × 100** (centi-lot), so `drops` = a car park *filling up*; `qty` = total car lots (capacity); deal "fullrisk" = ≤ 10 free car spaces (nearly full). A park gone from the feed pauses its series. money() renders centi-lots as $ in the 2 hardcoded spots | `available`, `total`, `by_type` (lot_type -> `{avail,total}`), `updated` | `carpark_number` |

### utilities & outages
| source | `price_cents` denomination | `flags` keys | `extra` keys |
|--------|----------------------------|--------------|--------------|
| outages | **not money** — `price_cents` = **customers affected × 100** (centi-customer), so `drops` = an outage *shrinking* (customers restored in stages); `qty` = crew-status ordinal (reported 1 → assigned 2 → attending 3 → Partially Restored 4 → Restored 5, shared across networks); deal "major" = **unplanned** & ≥ 100 customers. Join key = composite `network:<outage id>` (`--cc` picks the network: `powercor` VIC AU point, `energex` SE QLD AU point, `westernpower` WA AU polygon, `mbhydro` Manitoba Hydro CA polygon, `bchydro` British Columbia CA point); an id gone from the feed = restored (fetch returns nothing, the series ends). mbhydro/energex/bchydro epoch-ms dates render UTC `YYYY-MM-DD HH:MMZ`; mbhydro's `11111111` sentinel/test feature is skipped. energex `TYPE` (PLANNED/UNPLANNED) + westernpower `PLANNEDOUTAGE` (Planned/Unplanned) are explicit planned flags; westernpower serves no crew-status field (ordinal defaults mid) and its start/ETR are local date strings, not epoch-ms; bchydro join key = `GlobalID`, `CREW_STATUS` (dispatched/en_route/arrived) maps into the shared crew ordinal. money() renders centi-customers as $ in the watchlist + poll DROP line only (geonet/nzroads precedent) | `customers`, `status`, `cause`, `planned`, `etr`, `start`, `updated`, `network` + per-network extras (powercor: `town`, `lga`; mbhydro: `custtxt`, `subcause`, `type`, `etr_verified`; energex: `type`, `finish`) | `network`, `outage_id`, `lat`, `lon`, `url` + powercor: `town`, `area`, `postcode`, `lga`, `street`; energex: `town` (suburb), `street` |

### marine & coastal
| source | `price_cents` denomination | `flags` keys | `extra` keys |
|--------|----------------------------|--------------|--------------|
| noaatides | **not money** — `price_cents` = **water level (ft above MLLW) × 100**, so `drops` = the water *falling* (ebb tide); `qty` = null; deal "hightide" = `rising` and within 5% of the 24h max. money() renders centi-feet as $ in the 2 hardcoded spots | `level_ft`, `datum`, `rising`, `near_high`, `max_24h`, `min_24h`, `range_24h`, `time`, `state` | `state`, `lat`, `lon`, `url` |
| ndbc | **not money** — `price_cents` = **significant wave height (m) × 100** (centi-metre), so `drops` = the seas *calming*; `qty` = wind speed (m/s, rounded); deal "bigswell" = WVHT ≥ 3 m. Missing readings are the `MM` sentinel (→ null). money() renders centi-metres as $ in the 2 hardcoded spots | `wvht_m`, `dom_period_s`, `wave_dir_deg`, `wind_ms`, `gust_ms`, `wind_dir_deg`, `water_temp_c`, `air_temp_c`, `pressure_hpa`, `time` | `lat`, `lon`, `name`, `url` |

### civic & government
| source | `price_cents` denomination | `flags` keys | `extra` keys |
|--------|----------------------------|--------------|--------------|
| civic311 | **not money** — `price_cents` = the request's **age in hours** since created (the wait clock); `qty` = a status ordinal (open 1 / closed 3), so the core's `drops` doesn't fire — the Open→Closed flip shows in `flags.status`/`status_norm` on the next poll. deal "stale" = still open after 7+ days (168h). Join key = composite `city:requestid` (`--cc` = nyc/chicago/sf); default board = oldest-open within the last 30 days (the current backlog, not never-closed zombie records); each city has a field adapter (Socrata columns differ). money() renders age-hours as $ in the 2 hardcoded spots | `city`, `type`, `status`, `status_norm` (open/closed), `created`, `age_hours`, `agency` | `city`, `request_id`, `where`, `agency` |

### jobs & labour
| source | `price_cents` denomination | `flags` keys | `extra` keys |
|--------|----------------------------|--------------|--------------|
| arbeitnow | **not money** — `price_cents` = the posting's **age in hours** since `created_at` (its time-on-board); `qty` = the tag count; deal "fresh" = posted within the last 48h. Join key = job `slug`; one memoized GET (latest ~100 postings); a slug gone from the feed = filled/expired (fetch returns nothing, the series ends — the listing-lifecycle hoard). money() renders age-hours as $ in the 2 hardcoded spots | `company`, `location`, `remote`, `age_hours`, `created_at`, `tags` (csv), `job_types` (csv) | `company`, `location`, `remote`, `url`, `tags`, `job_types` |

All money is integer **cents**. `price_cents = 0` means free; `NULL`/empty means the source returned
no price for that observation. Currencies differ by source and `--cc`; the denomination is not stored
per-row, so record which `--cc` a source is polled with if you mix them downstream.
